# CodeBuddy 使用指南 — 智能体模式

> 来源: https://www.codebuddy.cn/docs/ide/  
> 抓取日期: 2026-07-13

---

## 概览

快速导航
基础能力
核心能力
快速开始
快捷唤起
基础交互
智能工作流
1. 核心交互模式
2. 自定义 Agent
进阶特性
集成与生态 (Integrations)
​​​ 概述
​

CodeBuddy IDE 深度融合了设计、编码、调试与部署全流程，旨在通过高度自 动化的工作流，为开发者打造丝滑的 AI 协作体验。

基础能力
​

CodeBuddy 覆盖了从创意萌芽到代码上线的全生命周期：

智能对话：即时技术咨询，支持多轮对话与复杂逻辑推导。

代码补全：毫秒级代码预测，深度适配项目私有风格。

Figma 转代码：一键将 UI 设计稿转化为可维护的高质量前端代码。

组件库集成：预置企业级 UI 组件，支持自然语言调整样式与布局。

智能诊断：自动发现潜在 Bug 与性能瓶颈，提供一键修复方案。

核心能力
​

CodeBuddy 代码补全 Pro：引入 NES (Next Edit Suggestions) 前瞻式编辑能力，重新定义智能补全， 了解更多。

核心亮点：超越光标，预见下一步

突破传统补全局限，NES 不仅补全当前行，更能预测接下来的编码意图，自动对相关联的后续代码块（如下一行逻辑、函数体结尾）给出智能建议，真正实现 “写一补十” 的前瞻式体验。

无缝集成：默认开启，随心触发

功能默认开启，支持自动预判与手动触发双模式。手动触发快捷键：Ctrl + Shift + Enter, 一键召唤灵感，让编码行云流水。

快速开始
​
快捷唤起
​
操作系统	快捷键	功能描述	快捷键	功能描述
macOS	⌘ + I	内联对话	Ctrl + ⌘ + I	打开侧边栏对话
Windows	Ctrl + I	内联对话	Ctrl +Win +I	打开侧边栏对话
基础交互
​
新建会话：点击顶部的 + 图标。
上下文引用：在输入框中输入 @ 即可引用文件、文件夹或当前代码片段。
多模态输入：支持直接粘贴截图或导入 Figma 链接进行 UI 开发。
智能工作流
​

CodeBuddy 预置了多种专业模式，并支持自定义扩展，以适配不同的开发需求：

1. 核心交互模式
​

Ask (对话模式)：专注于技术问答与概念解释，不直接修改代码。适合快速获取思路或查阅 API。

Craft (Agent模式)：专注于当前上下文的代码生成与局部修改。响应迅速，适合精准的函数编写或小范围重构。

Plan (计划模式)：最强自动化模式。适用于跨文件修改或复杂功能开发。AI 会先制定任务清单（Plan），经您确认后自主执行读取、修改及环境检查。了解更多 Plan 模式。

2. 自定义 Agent
​

除了预置模式，您还可以点击 「+ 创建 Agent」，根据特定业务逻辑或工作习惯，定制专属的 AI 智能体，实现更高程度的流程自动化。了解更多 自定义 Agent。

进阶特性
​

CodeBuddy 的强大之处在于其可配置与可扩展的“大脑”：

Memory (记忆偏好)：自动记录您的偏好与项目关键背景，让 AI 随时间推移变得越来越精，更多参考Memory。

Rules (规则)：通过自定义 .codebuddy/rules，强制 AI 遵循特定的架构设计或编码规范，更多参考Rules。

Skills (技能库)：通过用户/项目级配置 .codebuddy/skills/, 针对特定场景（如安全审计、性能压测）封装的专业指令集，更多参考Skills。

MCP (模型上下文协议)：开放协议，支持接入第三方工具（如数据库、实时搜索）作为 AI 的辅助工具，更多参考MCP。

自定义模型（API接入）：支持自定义模型（API 接入），实现更丰富的 AI 能力，更多参考Models。

自定义指令（个性化适配）：支持个性化开发习惯，让代码补全贴合个人 / 团队习惯与专属业务场景，更多参考自定义指令。

集成与生态 (Integrations)
​

CodeBuddy 与主流开发生态深度打通，实现一站式研发闭环：

云服务集成：原生支持 CloudBase、Supabase、CloudStudio，实现数据库管理与一键预览部署。

前端组件集成：支持通过选择标准化组件库规范，直接生成符合企业级设计系统的可维护前端代码。

设计组件集成：支持通过 Add from Figma 功能按钮从内置的 Figma 中选择设计稿导入到 IDE 中，可准确将 Figma 设计稿转换为生产代码。 了解更多

小贴士：您可以根据任务复杂度随时在 Ask、Craft、Plan 模式间切换。对于需要大范围改动代码的任务，可以尝试使用 Plan 模式！

最后更新: 2026/1/21 15:41

Pager
上一页
快速开始
下一页
概览

---

## 智能体概览

快速导航
适合的场景
建议阅读顺序
智能体模式
​

智能体模式是 CodeBuddy IDE 中面向复杂任务协作的一套工作方式。您可以直接在 IDE 内创建任务、持续对话、查看执行结果，并在同一个界面中完成文件检查、代码审查和结果预览。

适合的场景
​
需要分步骤推进的开发任务
需要边聊边改、持续追问的任务
需要查看产物、文件、变更和预览结果的任务
需要同时处理多个任务的场景
建议阅读顺序
​

快速开始
先了解如何进入智能体模式、发起第一个任务，以及界面的基本结构。

创建任务
了解如何选择工作模式、填写任务内容和补充上下文。

任务管理
了解如何查看任务列表、按状态筛选，以及继续处理已有任务。

任务对话
了解如何在对话区持续追问、上传文件，并使用顶部操作。

结果查看
了解如何在右侧结果区查看产物、全部文件、变更和预览。

最后更新: 2026/3/9 08:18

Pager
上一页
概览
下一页
快速开始

---

## 智能体快速开始

快速导航
产品概述
什么是 CodeBuddy Agents
核心优势
适用场景
环境要求
在 CodeBuddy IDE 中进入智能体模式
发起您的第一个任务
各区域功能简介
快速开始
​
产品概述
​
什么是 CodeBuddy Agents
​

CodeBuddy Agents 是 CodeBuddy IDE 中的 Agents 模式，面向智能体开发场景设计。您可以直接在 IDE 中与智能体协作，把需求拆分为多个任务并行推进，并在同一界面中查看产物、全部文件、代码变更和预览结果。

核心优势
​
多任务并行：支持同时发起和管理多个智能体任务，适合并行探索、编码、修复和文档整理。
更适合审查：通过产物、全部文件和变更等视图，更清晰地审查智能体的执行过程和产出。
结果预览更直接：前端和应用类任务可结合预览视图快速查看运行效果，边看边调。
适用场景
​

编程模式 -- 面向代码开发场景：

日常编码辅助：代码生成、代码审查、Bug 修复、重构优化。
项目理解：快速了解陌生代码库的架构和逻辑。
网站/应用开发：从需求到代码的全流程辅助。

工作模式 -- 面向泛开发者的通用工作场景（默认模式）：

文档生成：生成 README、API 文档、技术设计文档等。
数据分析：上传数据文件，让智能体帮您分析和可视化。
PPT/报告生成：从需求描述生成演示文稿和报告。
深度研究：对复杂问题进行深度调研并输出报告。
邮件编辑、周报生成等办公场景。
环境要求
​
IDE：已安装并打开 CodeBuddy IDE。
项目：已在 IDE 中打开需要协作的本地项目。
网络：需要网络连接以调用 AI 模型服务。
账号：已注册并登录 CodeBuddy 账号。
在 CodeBuddy IDE 中进入智能体模式
​
打开 CodeBuddy IDE，并进入您要协作的项目。
如果当前已经打开项目，可以通过右上角的 打开 Agents 入口直接进入智能体模式。
也可以在 IDE 首页点击工作模式卡片底部的 开始工作 按钮进入智能体模式。
如果您尚未登录，系统会显示登录引导页面。点击登录按钮完成登录。

发起您的第一个任务
​
进入智能体界面后，您将看到新建任务页面，包含欢迎语、输入框和任务入口。
页面默认处于 工作模式。如果您需要进行代码开发，可以切换到 编程模式。
在输入框中输入您的需求描述，例如："帮我在当前项目中添加一个用户登录功能"。
您可以选择运行模式和 AI 模型。
点击发送按钮或按回车键，智能体即开始执行任务。
智能体会自动使用您当前 IDE 打开的项目和分支作为工作上下文，无需额外配置。
您可以继续新建其他任务，让多个智能体任务并行推进，并在侧边栏中随时切换查看。

各区域功能简介
​
区域	功能
侧边栏	显示任务列表（按文件夹分组），支持搜索和管理。底部显示用户头像。
对话区	核心交互区域，包含任务标题栏、消息列表和输入框。
上下文区	右侧面板，展示产物、全部文件、变更、预览等视图。可通过任务标题栏中的切换按钮展开或收起。

最后更新: 2026/3/9 08:18

Pager
上一页
概览
下一页
创建任务

---

## 创建任务

快速导航
选择工作模式
填写任务内容
选择工作目录
添加上下文
任务开始后
创建任务
​
选择工作模式
​

新建任务时，您需要先选择工作模式。当前界面提供两种模式：

模式	说明	默认
工作模式	面向文档、报告、调研、数据分析等通用工作场景	默认选中
编程模式	面向代码开发场景，适合实现功能、修复问题、重构代码等任务	-

模式差异：

任务类型不同：工作模式更适合内容生产和通用协作，编程模式更适合代码相关任务。
工作目录不同：编程模式通常需要指定本地项目目录，工作模式可以直接开始。
界面记忆：系统会记住您上次选择的模式，下次创建任务时自动回填。

填写任务内容
​

您可以通过以下方式开始创建任务：

点击左侧的 新建任务 按钮，进入新建任务页面。
选择 工作模式 或 编程模式。
在输入框中直接描述您的需求，例如“帮我在当前项目中添加一个用户登录功能”。
根据需要调整输入框下方的运行方式和模型。
点击发送按钮或按回车键，智能体开始执行任务。
选择工作目录
​

在 编程模式 下，建议为任务指定本地工作目录：

点击输入框右下角的 选择文件夹，选择当前任务要使用的目录。
选择完成后，智能体会基于该目录读取、分析和修改代码。
如果您经常在同一个项目中工作，可以优先选择当前已打开的项目目录，减少重复切换。
添加上下文
​

在创建任务时，您可以补充上下文，让智能体更快理解需求：

方式	说明
引用上下文	通过 @ 引用文件、文档、规则等信息
粘贴截图	使用 Ctrl/Cmd + V 直接粘贴剪贴板中的图片
上传文件	点击上传按钮或拖拽文件到输入框
补充说明	在任务描述中明确目标、范围、约束和预期输出

建议优先补充以下信息：

目标是什么：要实现什么功能，或要产出什么内容。
范围是什么：只改当前模块，还是需要联动多个目录。
约束是什么：技术栈、风格要求、命名规范、截止时间等。
结果标准是什么：您希望看到代码、文档、分析结论，还是可预览的页面。
任务开始后
​

任务创建成功后：

新任务会出现在左侧任务列表中。
对话区会持续展示智能体的执行过程和输出内容。
右侧上下文区会根据任务类型展示产物、全部文件、变更和预览等结果。
您可以继续补充消息，或同时创建其他任务并行推进。

最后更新: 2026/3/9 08:18

Pager
上一页
快速开始
下一页
任务管理

---

## 任务管理

快速导航
任务列表
搜索与筛选
任务状态
常见管理操作
继续处理已有任务
任务管理
​
任务列表
​

左侧边栏会显示您创建过的任务，方便您随时回看、继续处理和切换上下文。

当前列表通常包含两类信息：

最近任务：展示最近访问或最近更新的任务。
按工作空间整理的任务：方便您在同一个项目下集中查看历史任务。

每个任务卡片通常会显示：

任务标题
当前状态
最近更新时间或创建时间

搜索与筛选
​

在任务列表顶部，您可以使用搜索和筛选功能快速定位任务：

搜索框：按任务标题快速查找任务。
筛选按钮：打开筛选面板，按状态或日期筛选任务。

常见筛选方式包括：

按状态筛选：例如进行中、已完成、失败等。
按日期筛选：缩小到某一时间范围内的任务。
重置筛选：清空当前筛选条件，恢复查看全部任务。

任务状态
​

任务会随着执行过程在不同状态之间变化。当前界面中常见的状态包括：

状态	说明
进行中	智能体正在执行任务
已完成	任务已经执行完成
失败	执行过程中出现问题，任务未能完成
待处理	任务等待进一步处理或输入
规划中	智能体正在分析需求并整理执行思路
已归档	任务已被归档，用于后续查阅
常见管理操作
​

在任务列表中，您可以进行以下操作：

点击任务卡片，进入该任务的对话和结果页面。
通过搜索或筛选，快速找到特定任务。
在已有任务基础上继续发送消息，让智能体接着之前的上下文继续处理。
对不再需要的任务执行清理操作。
继续处理已有任务
​

对于已经完成、失败，或暂时中断的任务，您都可以重新进入任务继续补充消息。这样可以在已有上下文基础上继续推进，而不必重新从头创建。

适合继续处理的场景包括：

任务第一次结果还不够完整，想继续细化。
任务执行失败后，需要补充信息再试一次。
您想基于之前的结果继续扩展新的需求。

最后更新: 2026/3/9 08:18

Pager
上一页
创建任务
下一页
任务对话

---

## 任务对话

快速导航
对话方式
顶部操作
消息发送与继续追问
文件和图片上传
执行过程展示
中断与继续
任务对话
​
对话方式
​

任务创建后，您会在中间区域与智能体持续对话。这里的使用方式与 IDE 内侧边栏中的交互习惯基本一致，都是围绕“输入需求、补充上下文、查看执行过程、继续追问”展开。

常见操作包括：

在输入框中直接输入需求或补充说明。
点击发送按钮或按回车键发送消息。
在已有任务中继续追问，让智能体接着上一次的上下文继续处理。
在任务执行过程中查看回复、结果和中间步骤。

顶部操作
​

对话区顶部除了显示当前任务标题，还提供一些常用操作，方便您在任务处理中快速切换和保持上下文。

打开编辑器：快速打开当前任务关联的编辑器或项目内容，方便一边看对话，一边回到代码或文件继续处理。
保留工作空间：在同一个项目中持续追问或反复修改时，可以保留当前工作空间，减少重复切换和重新选择。
消息发送与继续追问
​

在对话区底部的输入框中，您可以随时发送新消息。

适合直接发送的内容包括：

新需求：例如新增一个功能或补充一段说明。
修改意见：例如调整实现方式、补充限制条件。
继续追问：例如让智能体基于当前结果继续优化。

如果您已经在 IDE 内侧边栏中习惯了输入、发送、继续追问的操作方式，这里的体验基本一致，不需要重新学习。

文件和图片上传
​

除了直接输入文字，您也可以补充文件或图片作为上下文：

粘贴截图：使用 Ctrl/Cmd + V 直接粘贴剪贴板中的图片。
上传文件：点击上传按钮，或直接拖拽文件到输入框区域。
补充本地内容：结合文件、图片和文字说明，让智能体更快理解问题。
执行过程展示
​

智能体在执行任务时，会把关键步骤和结果展示在对话区中，方便您随时了解进展。

您通常会看到以下内容：

智能体的回复内容
执行中的阶段说明
已生成的结果摘要
可展开查看的中间步骤

这样做的好处是，您不需要离开当前任务，就能在同一处持续查看输入、过程和输出。

中断与继续
​

如果任务正在执行，输入框区域会显示停止入口，您可以随时中断当前执行。

中断后，您仍然可以：

继续补充新的说明
调整需求后重新发送
基于已有内容继续推进任务

这和 IDE 中常见的侧边栏式交互方式一致，重点是保持连续对话，而不是每次都重新开始。

最后更新: 2026/3/9 08:18

Pager
上一页
任务管理
下一页
结果查看

---

## 结果查看

快速导航
右侧结果区
产物
表格预览
文档预览
网页预览
全部文件
变更
结果查看
​
右侧结果区
​

任务执行过程中，右侧会显示结果区，用来查看智能体产出的文件、代码变更和预览内容。您不需要离开当前任务，就可以一边看对话，一边查看结果。

当前结果区主要提供四类内容：

产物：查看任务生成的文件和交付物
全部文件：查看当前工作空间中的文件内容
变更：查看本次任务带来的代码修改
预览：查看网页或可直接打开的结果页面

这些内容都集中在同一块区域中，方便您来回切换和对照。

产物
​

当任务生成文件、表格、文档或其他可交付内容时，您可以在 产物 中统一查看。

产物区通常包含两部分：

左侧是当前任务生成的产物列表
右侧是选中产物的预览区域

适合在这里查看的内容包括：

文档类结果
表格类结果
自动生成的交付文件

表格预览
​

如果任务生成的是表格文件，您可以直接在结果区中打开查看，不需要先下载再用外部工具打开。

这样更适合快速确认：

表头是否正确
数据是否完整
排版和格式是否符合预期

文档预览
​

如果任务生成的是文档文件，您也可以直接在结果区中查看内容。这样在交付前，可以先快速检查版式、标题和正文内容是否符合预期。

网页预览
​

如果任务生成的是网页、页面原型或本地启动的 Web 应用，您可以在 预览 中直接查看效果。

预览区通常会提供：

当前页面地址
刷新入口
页面内容的实时查看

这适合用于确认页面结构、样式和交互结果是否符合预期。

全部文件
​

全部文件 用于查看当前工作空间中的文件内容，适合在不离开任务的情况下直接浏览代码或产出文件。

这一视图通常包含：

左侧文件列表或文件树
右侧文件内容区域
顶部已打开文件的切换栏

适合用来：

快速查看某个生成文件的完整内容
对照不同文件之间的关系
检查任务是否写入了正确的位置

变更
​

变更 视图用于查看当前任务带来的代码修改，方便您在接受结果前先做一轮检查。

这一视图通常会显示：

本次修改涉及的文件列表
每个文件的改动情况
选中文件后的具体变更内容

如果任务涉及代码开发、脚本生成或配置调整，建议优先在这里确认改动是否符合预期。

最后更新: 2026/3/9 08:18

Pager
上一页
任务对话
下一页
Figma

---

## Figma

快速导航
功能概述
使用前提
操作步骤
1. 启动 Figma 集成
2. 授权登录 Figma
3. 确认授权成功
4. 选择设计文件
5. 选择设计组件
6. 开始代码生成
7. 预览生成结果
使用技巧
从 Figma 导入设计稿
​
功能概述
​

CodeBuddy IDE 提供强大的设计到代码转换功能，通过内置集成的 Figma2Code 功能，您可以直接从 Figma 设计稿生成高质量的生产代码。本功能支持精确的设计还原，大幅提升开发效率。

使用前提
​

⚠️ 重要提示：使用此功能需要满足以下条件：

Figma 账户权限：您需要拥有 Figma 设计文件的编辑或开发权限
文件访问权限：确保您有权访问要导入的 Figma 设计文件
团队协作权限：如果是团队项目，需要获得相应的协作权限

如果您没有相关权限，请联系设计文件的所有者或团队管理员获取访问授权。

操作步骤
​
1. 启动 Figma 集成
​

在 Craft Agent 界面中，点击 Figma 按钮开始导入流程。

2. 授权登录 Figma
​

系统将跳转至 Figma 官网，请使用您的 Figma 账户进行登录授权。

3. 确认授权成功
​

登录完成后，您将看到授权成功的确认页面。

4. 选择设计文件
​

从您的 Figma 工作区中选择需要的设计文件，点击打开进入编辑界面。

5. 选择设计组件
​

在 Figma 的 Layers 面板中：

选择您需要转换的组件或设计区域
点击右上角的 Add to Conversation 按钮
所选设计将被导入到 CodeBuddy IDE 中

6. 开始代码生成
​

设计稿成功导入后，将在 IDE 对话框中显示。现在您可以：

在输入框中描述具体的开发需求
AI 将基于设计稿自动生成对应的代码

7. 预览生成结果
​

代码生成完成后，您可以在 Preview 面板中查看效果。如图所示，生成的代码与原设计稿保持了极高的还原度。

使用技巧
​
精确选择：在 Figma 中选择组件时，建议选择完整的设计模块以获得更好的转换效果
描述需求：在代码生成时，详细描述功能需求和技术栈偏好，AI 将生成更符合预期的代码
迭代优化：可以基于生成的代码继续与 AI 对话，进行功能完善和样式调整

通过 Figma 集成功能，设计师和开发者可以无缝协作，快速将设计理念转化为可运行的代码。

最后更新: 2026/1/28 19:16

Pager
上一页
结果查看
下一页
代码补全

---

