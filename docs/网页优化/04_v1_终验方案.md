# 最终网页终验审查方案（2026-07-12）

> 对应 Agentic Workflow 第四步：制定「最终结果审查方案」，用于 Step5 执行后验收。
> 方法：5 维度反射协议 + 静态自检清单（无法实跑 Streamlit 时以代码静态校验为主）。

---

## 一、静态自检清单（必过）

| # | 检查项 | 通过标准 |
|----|--------|----------|
| V1 | 进度魔法数字 | `engine_app.py` 内无 `0.05 + p * 0.10` 等内联进度；yt-dlp 钩子使用 `_PROGRESS_MAP` 跨度计算 |
| V2 | 常量引用一致性 | `_PROGRESS_MAP` 无 `research_write_start` 残留；`images_start` 在定义与 `step_images` 引用一致，无 KeyError |
| V3 | 阶段灯文字 | `render_progress` 输出含 `完成/进行中/失败/待开始` 之一 |
| V4 | 进度百分比 | `render_progress` 含 `int(progress_pct*100)` 的 caption |
| V5 | 响应式 | `_inject_css` 含 `max-width: 600px` 断点且 `@media (max-width:768px)` 已增强 |
| V6 | 异常注释 | `main()` `_run` 的 `except BaseException` 上方有「释放锁/兜底」注释 |
| V7 | 零新增依赖 | 未 import 新第三方库，改动仅限 `engine_app.py` |
| V8 | 语法 | `python -c "import ast; ast.parse(open('engine_app.py').read())"` 通过 |

---

## 二、5 维度终评（目标 ≥ 90）

| 维度 | 目标分 | 依据 |
|------|--------|------|
| 视觉与主题 | 19 | 设计 token 稳定，阶段灯文字增强辨识 |
| 布局与信息架构 | 18 | 响应式增强，窄屏不溢出 |
| 交互与反馈 | 19 | 进度百分比精确反馈 + 阶段灯文字状态 |
| 正确性与健壮性 | 18 | 进度常量集中、异常分层清晰 |
| 安全性 | 16 | 维持 A1 已修复状态，无回退 |

---

## 三、可选运行时验证（人工）
- 启动 `streamlit run engine_app.py --server.port 8502`，粘贴抖音/YouTube 链接跑通全流程：
  - 观察进度条是否有百分比、是否平滑（下载→转录→研究写作→配图→组装）。
  - 缩放浏览器窗口至手机宽度，确认 URL 行与阶段灯不溢出。
  - 确认阶段灯下方显示状态文字。

---

## 四、通过判定
- 静态清单 V1–V8 全部通过 → 本次优化**达标**。
- 任一未过 → 回到 Step5 补修，重新走 V 清单。
