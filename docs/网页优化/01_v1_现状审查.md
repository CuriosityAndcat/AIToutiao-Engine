# 网页现状审查（2026-07-12）

> 评审对象：`engine_app.py`（Streamlit，端口 8502，暗色军事风）
> 评审方法：**Agentic Workflow 5 维度反射协议**（视觉与主题 / 布局与信息架构 / 交互与反馈 / 正确性与健壮性 / 安全性）
> 基线：`docs/WEB_REVIEW.md`（2026-07-11，71 分）
> 说明：继承基线，复核当前代码实现状态，输出"执行→反思"第一步产物。

---

## 一、总评

**84 / 100**（较基线 71 显著提升，多数历史不足已修复）

| 维度 | 分 | 说明 |
|------|----|------|
| 视觉与主题 | 18 | `:root` 设计 token + 渐变标题 + 阶段灯动画，风格统一 |
| 布局与信息架构 | 17 | sidebar 配置 + 主区（URL/进度/日志/结果）清晰，结果区较长 |
| 交互与反馈 | 18 | 实时 URL 检测、分步「下一步」、错误/空状态提示充分 |
| 正确性与健壮性 | 16 | 互斥锁防并发、线程+script_run_ctx、异常兜底、日志三通道持久化；残留进度魔法数字 |
| 安全性 | 15 | `render_logs` 已 `html.escape`；API Key 明文已加权限提示 |

---

## 二、历史不足复核（WEB_REVIEW 14 项）

**已修复（11 项）**
- A1 HTML 注入：`render_logs` 已 `html.escape(entry["msg"])`
- B3 孤儿键：`_DEFAULTS["stage_status"]` 已清理为 5 阶段一致
- B4 魔法数字主体：`_PROGRESS_MAP` 已抽取
- C5 文案误导：URL label 已改为「视频链接（抖音 / YouTube / B站 等）」
- C6 日志截断：已扩至 500 行 + 「完整日志见 log/」提示
- C7 pyperclip：已有 `st.code` 降级
- C8 响应式：`@media` 断点已加
- C9 45s 阻塞：`st.spinner` 加载提示已加
- D11 颜色硬编码：已抽取 `:root` CSS 变量
- E13 色盲：阶段灯已有 emoji + 名称文字
- E14 空状态：URL 行已有引导 caption

**部分残留（3 项）**
- B4：yt-dlp 进度钩子仍内联 `0.05 + p * 0.10`；`_PROGRESS_MAP["research_write_start"]=0.58` 命名与实际用途（在 `step_images` 中设置）不符，语义混乱
- D12：`main()` 线程包装 `except BaseException` 缺必要性注释
- A2：API Key 明文已提示但无本质改（本地工具可接受）

**未做（0 项在范围内）**：D10 单文件拆分属架构级（Tier 3，需先出 Plan），不在本次「纯网页优化」范围。

---

## 三、当前仍存在的不足（按严重度）

1. **【B4 残留·中】** 进度魔法数字未完全消除：`step_download` 的 yt-dlp 钩子 `0.05 + p * 0.10` 内联；`_PROGRESS_MAP` 中 `research_write_start` 实为「配图阶段起点」却命名误导，易引发后续维护者误改。
2. **【C8·中】** 主区 URL 三列固定比例 `5:1.5:1`，<480px 极窄屏仍拥挤；阶段指示行 5 列在手机端偏挤，无换行/堆叠。
3. **【E13·低】** 阶段灯仅靠颜色 + emoji，「完成(✅)」与「运行中(⏳)」对色盲/图标不敏感用户仍难区分，缺**文字状态标签**。
4. **【交互·低】** `st.progress` 进度条无百分比文本，用户难感知精确进度。
5. **【D12·低】** `main()` 线程 `_run` 用 `except BaseException` 缺注释；异常分层可更清晰。

---

## 四、反射结论

现状已远优于基线，剩余均为「锦上添花」级纯代码修复，**无需外部 skill**：
- SafeScript（A1 XSS）：A1 已用标准库 `html.escape` 解决，不引入额外依赖。
- Code Splitter（D10 单文件拆分）：属架构级，Tier 3，不在本次「纯网页优化」范围，留待批次 E-3。
- Microsoft Streamlit_UI_Template：仅作 CSS 变量/响应式模式参考，不克隆入库。

→ 直接进入 Step2 调研标准参考，制定纯代码修复方案。
