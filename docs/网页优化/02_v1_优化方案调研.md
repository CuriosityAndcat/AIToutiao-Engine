# 优化方案调研（2026-07-12）

> 对应 Agentic Workflow 第二步：查询「如何去优化这个项目」——是否有标准参考 / GitHub 标准 skill / 官方规范。
> 输入：Step1 现状审查（5 项残留不足）

---

## 一、标准参考（权威性排序）

| 来源 | 类型 | 适用点 | 结论 |
|------|------|--------|------|
| **Streamlit 官方 Theming** (`docs.streamlit.io/.../theming`) | 官方规范 | 主题统一、CSS 自定义属性 | ✅ 采用：用 CSS 变量（custom properties）做设计 token，与现有 `:root` 体系一致 |
| **Streamlit Component Theming**（CSS custom properties + 响应式） | 官方规范 | 响应式布局模式 | ✅ 采用：媒体查询 + 容器约束 |
| **Microsoft Streamlit_UI_Template**（GitHub） | 参考项目 | 颜色 token / 组件化 CSS | ✅ 仅参考其 CSS 变量体系，不克隆 |
| **awesome-streamlit-themes**（GitHub） | 主题集合 | 暗色主题范式 | 🟡 参考，不引入第三方主题 |

---

## 二、GitHub Skill / 工具匹配

| Step1 不足 | 是否有精准 skill | 命中 | 采用判定 |
|------------|------------------|------|----------|
| B4 魔法数字 | ❌ 无专项目 | — | 纯代码常量提取，手写即可 |
| C8 响应式 | ❌ 无精准 | — | 手写 `@media`（已有基础，扩展） |
| E13 色盲文字标签 | ❌ 无精准 | — | 手写状态文字映射 |
| 交互百分比 | ❌ 无精准 | — | 手写 caption |
| D12 异常注释 | ❌ 无精准 | — | 手写注释 |

**检索结论**：与 WEB_REVIEW 一致——本批 5 项不足均为**纯代码修复**，SkillHub / GitHub 均无精准专项 skill。沿用「本引擎已有能力直接修复」策略，不引入外部依赖。

---

## 三、方案生成（候选）

1. **进度系统理顺（B4）**：
   - 在 `_PROGRESS_MAP` 增加 `download_start: 0.05`；yt-dlp 钩子改为基于 `download_start` 与 `transcribe_start` 跨度计算，消除 `0.05 + p*0.10` 内联。
   - 将 `research_write_start` 重命名为语义正确的 `images_start`（仅为配图阶段起点）。
2. **响应式增强（C8）**：扩展 `@media (max-width:768px)` / 新增 `600px` 断点，使 URL 行输入可堆叠、阶段指示行紧凑、卡片内图片列窄屏自适应。
3. **阶段灯文字状态（E13）**：在 `render_progress` 指示灯下加 `完成/进行中/失败/待开始` 文字标签，色盲友好。
4. **进度百分比（交互）**：进度条下方显示 `xx%` caption。
5. **异常分层注释（D12）**：`main()` 线程 `_run` 的 `except BaseException` 加必要性注释；`execute_pipeline` 异常分层加注释。

---

## 四、范围裁定

- **纳入**：上述 5 项纯代码修复（均在 `engine_app.py`，不涉及 `write_stage.py` 生产逻辑，不破坏 backend 解耦）。
- **排除**：D10 单文件拆分（架构级，Tier 3，批次 E-3）；SafeScript（A1 已修）；新增第三方主题/库（保持军事风统一）。

→ 进入 Step3 审查并定稿方案。
